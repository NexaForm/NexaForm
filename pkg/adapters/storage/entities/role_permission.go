package entities

/* we need it or not? */

// type RolePermission struct {
// 	RoleID       uuid.UUID `gorm:"primaryKey"`
// 	PermissionID uuid.UUID `gorm:"primaryKey"`
// 	CreatedAt    time.Time
// }
